@model IEnumerable<ToolsForEverApplication.Models.Voorraad>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Artikel.Product)
        </th>
        <th>
            Locatie
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Aantal)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Artikel.Product)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Locatie.Locatie1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Aantal)
        </td>
        <td>
            @Html.ActionLink("Voorraad toevoegen", "Edit", new { id=item.Productcode })
        </td>
    </tr>
}

</table>
