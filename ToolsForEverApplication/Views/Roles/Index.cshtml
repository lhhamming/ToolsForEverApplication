@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>
@{
    ViewBag.Title = "Add Role";
}
<table style=" background-color:#FFFFFF; border: dashed 3px #6D7B8D; padding: 5px;width: 99%;table-layout:fixed;" cellpadding="6" cellspacing="6">
    <tr style="height: 30px; background-color:#336699 ; color:#FFFFFF ;border: solid 1px #659EC7;">
        <td align="center" colspan="2">
            <h2> Maak een Gebruiker Role aan</h2>
        </td>

    </tr>
    <tr>
        <td>
            <table id="tbrole" style="width:100%; border:solid 1px; background-color:gainsboro; padding-left:10px;">

                @foreach (var item in Model)
                {
                    <tr>
                        <td style="width:100%; border:dotted 1px;">
                            @item.Name
                        </td>
                    </tr>}
            </table>
        </td>
        <td align="right" style="color:#FFFFFF;padding-right:10;">

            <h3>   @Html.ActionLink("Click to Create New Role", "Create", "Roles") </h3>
            <h3>   @Html.ActionLink("Click to add A Role to an User", "Link", "Roles")</h3>
            <h3>   @Html.ActionLink("Click to Remove a Role", "Remove", "Roles")</h3>
        </td>
    </tr>
</table>